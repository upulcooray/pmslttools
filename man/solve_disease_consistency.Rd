% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disease-consistency.R
\name{solve_disease_consistency}
\alias{solve_disease_consistency}
\title{Solve disease parameter consistency and write PMSLT-ready disease inputs}
\usage{
solve_disease_consistency(
  input_dir = "pmslt_inputs_raw",
  output_dir = file.path(input_dir, "disease_consistency_results"),
  solver = c("disbayes", "dismod_slove"),
  output_file = file.path(output_dir, "pmslt_disease_epi.csv"),
  overwrite = FALSE,
  horizon = NULL,
  ...
)
}
\arguments{
\item{input_dir}{Directory created by \code{\link[=draft_input_templates]{draft_input_templates()}}.}

\item{output_dir}{Directory where solver outputs should be written.}

\item{solver}{Solver backend. \code{"disbayes"} requires the optional
\code{disbayes} package. \code{"dismod_slove"} runs without optional dependencies.}

\item{output_file}{CSV path for the canonical PMSLT-ready disease input.}

\item{overwrite}{Logical. Should existing output files be overwritten?}

\item{horizon}{Optional simulation horizon. If \code{NULL}, inferred from
\verb{08_risk_factor_prevalence.csv} when available; otherwise 0.}

\item{...}{Additional arguments passed to the selected solver.}
}
\value{
Invisibly returns a list containing solver outputs,
\code{pmslt_disease_epi}, \code{output_dir}, \code{output_file}, and \code{solver}.
}
\description{
This is the beginner-facing bridge from checked raw disease inputs to the
canonical \code{pmslt_disease_epi.csv} file. \code{solver = "disbayes"} is the planned
primary real consistency solver and remains optional because it requires a
Stan-capable \code{disbayes} installation. \code{solver = "dismod_slove"} keeps the
package-native deterministic solver available for local workflows.
}
\examples{
\dontrun{
solve_disease_consistency("inputs_raw")
solve_disease_consistency("inputs_raw", solver = "dismod_slove")
}
}
