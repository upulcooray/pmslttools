% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main-lifetable.R
\name{compare_pmslt_results}
\alias{compare_pmslt_results}
\title{Compare intervention PMSLT results against BAU results}
\usage{
compare_pmslt_results(
  bau_results,
  intervention_results,
  by = c("overall", "time_step", "sex", "stratum", "age", "age_band")
)
}
\arguments{
\item{bau_results}{BAU output from \code{\link[=run_pmslt_lifetable_bau]{run_pmslt_lifetable_bau()}} or
\code{\link[=integrate_disease_deltas]{integrate_disease_deltas()}}.}

\item{intervention_results}{Intervention output with the same \code{time_step},
\code{age}, \code{sex}, and \code{stratum} structure as \code{bau_results}.}

\item{by}{Character vector of grouping variables. Use \code{"overall"} for one
ungrouped comparison row, or any combination of \code{"time_step"}, \code{"sex"},
\code{"stratum"}, \code{"age"}, and \code{"age_band"}.}
}
\value{
A plain data frame with grouping columns followed by difference
metrics. Differences are calculated as \code{intervention - BAU}.
}
\description{
Creates beginner-friendly intervention-minus-BAU summary tables from two
compatible PMSLT lifetable outputs. This is a reporting helper only: it does
not simulate an intervention or change the exact-age lifetable engine.
}
\examples{
population <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  population = c(1000, 900)
)
mortality <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  mortality_rate = c(0.01, 0.02)
)
bau <- run_pmslt_lifetable_bau(population, mortality, horizon = 1)
compare_pmslt_results(bau, bau)
}
