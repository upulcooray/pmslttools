% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{compare_costs}
\alias{compare_costs}
\title{Compare intervention and BAU deterministic PMSLT costs}
\usage{
compare_costs(
  bau_costs,
  intervention_costs,
  by = c("overall", "time_step", "sex", "stratum", "age", "age_band"),
  cost_cols = NULL,
  spec = NULL
)
}
\arguments{
\item{bau_costs}{BAU cost output.}

\item{intervention_costs}{Intervention cost output with the same
\code{time_step}, \code{age}, \code{sex}, and \code{stratum} structure as \code{bau_costs}.}

\item{by}{Character vector of grouping variables. Use \code{"overall"} for one
ungrouped row, or any combination of \code{"time_step"}, \code{"sex"}, \code{"stratum"},
\code{"age"}, and \code{"age_band"}.}

\item{cost_cols}{Optional character vector naming the cost columns to
summarise.}

\item{spec}{Optional \code{pmslt_spec} object used only for reporting
\code{age_band} when \code{costs} does not already carry a \code{spec} attribute.}
}
\value{
A plain data frame with grouping columns followed by cost
differences calculated as \code{intervention - BAU}.
}
\description{
Creates intervention-minus-BAU cost summaries from compatible cost outputs.
This is a reporting helper only: it does not discount costs, assign
thresholds, or change simulation outputs.
}
