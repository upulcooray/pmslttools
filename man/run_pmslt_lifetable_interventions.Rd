% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main-lifetable.R
\name{run_pmslt_lifetable_interventions}
\alias{run_pmslt_lifetable_interventions}
\title{Run BAU and intervention all-cause lifetables from disease effects}
\usage{
run_pmslt_lifetable_interventions(
  population,
  mortality,
  morbidity = NULL,
  intervention_effects,
  horizon = NULL,
  spec = NULL
)
}
\arguments{
\item{population}{Data frame or CSV path for main lifetable population.}

\item{mortality}{Data frame or CSV path for BAU all-cause mortality rates.}

\item{morbidity}{Optional data frame or CSV path for BAU all-cause morbidity
rates.}

\item{intervention_effects}{Data frame or CSV path returned by
\code{\link[=run_pmslt_interventions]{run_pmslt_interventions()}}.}

\item{horizon}{Number of yearly cycles to run. If omitted, uses
\code{spec$horizon} when \code{spec} is supplied, otherwise defaults to 1.}

\item{spec}{Optional \code{pmslt_spec} object.}
}
\value{
A list with class \code{pmslt_lifetable_interventions} containing \code{bau},
\code{interventions}, and \code{comparisons}. Comparison metrics use the
intervention-minus-BAU direction.
}
\description{
Bridges disease-level intervention outputs from \code{\link[=run_pmslt_interventions]{run_pmslt_interventions()}}
into the main all-cause lifetable. It returns one comparable BAU lifetable
and one intervention lifetable per intervention arm. This bridge is
deterministic only: it does not add costs, PSA, discounting, age weighting,
or equity logic.
}
\details{
Disease effects are first summed by \code{intervention}, \code{time_step}, \code{age},
\code{sex}, and \code{stratum}. In each intervention cycle, the all-cause mortality
rate is updated as \code{mortality_rate + sum(delta_mortality)} and clamped to
\verb{[0, 1]}; the all-cause morbidity rate is updated as
\code{morbidity_rate + sum(delta_morbidity)} and floored at zero. Deaths,
person-years, YLDs, survivors, and later-cycle ageing are then recalculated
from those adjusted all-cause rates.

Disease-specific long output is preserved in the \code{disease_deltas} attribute
on the BAU and intervention lifetables.
}
