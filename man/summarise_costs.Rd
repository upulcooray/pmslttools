% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{summarise_costs}
\alias{summarise_costs}
\title{Summarise deterministic PMSLT costs}
\usage{
summarise_costs(
  costs,
  by = c("overall", "time_step", "sex", "stratum", "age", "age_band"),
  cost_cols = NULL,
  group_by = NULL,
  spec = NULL
)
}
\arguments{
\item{costs}{Data frame with PMSLT reporting keys and one or more cost
columns. Cost columns are detected from \code{cost}, \code{costs}, \code{total_cost}, and
columns ending in \verb{_cost} or \verb{_costs}.}

\item{by}{Character vector of grouping variables. Use \code{"overall"} for one
ungrouped row, or any combination of \code{"time_step"}, \code{"sex"}, \code{"stratum"},
\code{"age"}, and \code{"age_band"}.}

\item{cost_cols}{Optional character vector naming the cost columns to
summarise.}

\item{group_by}{Optional alias for \code{by}.}

\item{spec}{Optional \code{pmslt_spec} object used only for reporting
\code{age_band} when \code{costs} does not already carry a \code{spec} attribute.}
}
\value{
A plain data frame with grouping columns followed by cost totals.
}
\description{
Summarises cost columns from an existing PMSLT-style output table. This is a
reporting helper only: it does not discount costs, assign thresholds, or
change simulation outputs.
}
