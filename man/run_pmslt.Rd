% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-pmslt.R
\name{run_pmslt}
\alias{run_pmslt}
\title{Run a complete PMSLT analysis from one call}
\usage{
run_pmslt(
  input_dir = NULL,
  spec = NULL,
  solver = c("dismod_slove", "disbayes"),
  horizon = NULL,
  disease_epi = NULL,
  population = NULL,
  mortality = NULL,
  morbidity = NULL,
  risk_prevalence = NULL,
  relative_risks = NULL,
  direct_effects = NULL,
  costs = NULL,
  stratum_rate_ratios = NULL,
  equity = FALSE,
  psa = FALSE,
  psa_draws = 100,
  psa_seed = NULL,
  psa_interval_width = 0.95,
  report_by = "overall",
  cohort_size = 1000,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{input_dir}{Directory created by \code{\link[=draft_input_templates]{draft_input_templates()}} holding the
numbered raw input CSVs. Used to locate any input not supplied explicitly.}

\item{spec}{Optional \code{\link[=pmslt_spec]{pmslt_spec()}} object. When supplied it validates labels
and supplies the default \code{horizon}.}

\item{solver}{Disease-consistency solver. \code{"dismod_slove"} (default) runs
without optional dependencies; \code{"disbayes"} runs the Bayesian solver and
needs the optional \code{disbayes} package. Ignored when \code{disease_epi} is given.}

\item{horizon}{Simulation horizon in years. Defaults to the spec horizon, or
is inferred by the solver.}

\item{disease_epi}{Optional pre-solved canonical disease inputs (data frame or
path to \code{pmslt_disease_epi.csv}). When supplied the solver step is skipped.}

\item{population, mortality, morbidity}{All-cause main-lifetable inputs. Default
to \verb{01_population.csv}, \verb{02_all_cause_mortality.csv}, and
\verb{03_all_cause_morbidity.csv} in \code{input_dir}. \code{population} and \code{mortality}
are required; \code{morbidity} is optional.}

\item{risk_prevalence, relative_risks, direct_effects}{Intervention evidence.
Default to \verb{08_risk_factor_prevalence.csv}, \verb{09_relative_risks.csv}, and
\verb{10_direct_intervention_effects.csv} in \code{input_dir} when present.}

\item{costs}{Optional cost inputs (\verb{12_costs.csv}). When supplied, costed
results and ICERs are produced.}

\item{stratum_rate_ratios}{Optional equity rate ratios
(\verb{11_stratum_rate_ratios.csv}). Used only when \code{equity = TRUE}.}

\item{equity}{Logical. Add a stratum-disaggregated disease-integration overlay
using \code{\link[=integrate_disease_deltas]{integrate_disease_deltas()}}.}

\item{psa}{Logical. Run a probabilistic sensitivity analysis with
\code{\link[=run_psa_interventions]{run_psa_interventions()}}.}

\item{psa_draws, psa_seed, psa_interval_width}{PSA controls passed through to
\code{\link[=run_psa_interventions]{run_psa_interventions()}}.}

\item{report_by}{Grouping for the headline HALY and cost summaries. Defaults
to \code{"overall"}.}

\item{cohort_size}{Disease-lifetable cohort size passed to
\code{\link[=run_pmslt_interventions]{run_pmslt_interventions()}}.}

\item{overwrite}{Logical. Overwrite an existing solver output file.}

\item{...}{Additional arguments passed to \code{\link[=solve_disease_consistency]{solve_disease_consistency()}}.}
}
\value{
An object of class \code{pmslt_run}: a list with the resolved \code{disease_epi},
per-arm intervention \code{deltas}, the \code{lifetable} bridge object (BAU plus each
intervention arm and their comparisons), \code{halys} summaries, optional \code{costs}
and \code{icers}, optional \code{equity} and \code{psa} elements, the \code{arms} modelled, and
run \code{metadata}. Use \code{\link[=summary]{summary()}} for a readable overview.
}
\description{
\code{run_pmslt()} is the end-to-end driver for the package. It chains the
individual workflow layers that are otherwise called separately:
}
\details{
\enumerate{
\item Solve disease-parameter consistency with \code{\link[=solve_disease_consistency]{solve_disease_consistency()}}
(or accept a pre-solved \code{disease_epi}).
\item Translate raw intervention evidence into per-arm disease deltas with
\code{\link[=run_pmslt_interventions]{run_pmslt_interventions()}}.
\item Age the population through the all-cause main lifetable for business as
usual and every intervention arm with \code{\link[=run_pmslt_lifetable_interventions]{run_pmslt_lifetable_interventions()}}.
\item Summarise health outcomes with \code{\link[=calculate_halys]{calculate_halys()}} / \code{\link[=compare_halys]{compare_halys()}},
attach costs with \code{\link[=attach_pmslt_costs]{attach_pmslt_costs()}}, and compute ICERs with
\code{\link[=calculate_icers]{calculate_icers()}}.
\item Optionally add an equity disaggregation overlay and a probabilistic
sensitivity analysis.
}

The function is deliberately beginner friendly: pointed at a directory created
by \code{\link[=draft_input_templates]{draft_input_templates()}}, it reads the standard numbered CSV files and
runs the whole pipeline with sensible defaults. Each input can also be passed
explicitly as a data frame or path, which is the escape hatch for advanced or
rigorous use.
}
\examples{
\dontrun{
out <- tempfile("pmslt_inputs_")
generate_mock_pmslt_inputs(output_dir = out)
run <- run_pmslt(out, solver = "dismod_slove", horizon = 5)
summary(run)
}
}
