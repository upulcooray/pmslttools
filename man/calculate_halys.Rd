% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main-lifetable.R
\name{calculate_halys}
\alias{calculate_halys}
\title{Calculate HALY-style health outcome summaries}
\usage{
calculate_halys(
  results,
  by = c("overall", "time_step", "sex", "stratum", "age", "age_band")
)
}
\arguments{
\item{results}{Output from \code{\link[=run_pmslt_lifetable_bau]{run_pmslt_lifetable_bau()}} or
\code{\link[=integrate_disease_deltas]{integrate_disease_deltas()}} with \code{person_years} and \code{yld} columns.}

\item{by}{Character vector of grouping variables. Use \code{"overall"} for one
ungrouped summary row, or any combination of \code{"time_step"}, \code{"sex"},
\code{"stratum"}, \code{"age"}, and \code{"age_band"}.}
}
\value{
A plain data frame with grouping columns followed by \code{halys},
\code{person_years}, and \code{yld}. Integrated disease-total summary columns are
preserved when they are available.
}
\description{
Creates a lightweight health-adjusted life-year style reporting summary from
existing PMSLT outputs. HALYs are calculated as \code{person_years - yld}. This is
a reporting helper only: it does not add discounting, age weighting, costs,
uncertainty intervals, DALYs, or change the lifetable engine.
}
\examples{
population <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  population = c(1000, 900)
)
mortality <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  mortality_rate = c(0.01, 0.02)
)
morbidity <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  morbidity_rate = c(0.12, 0.15)
)
bau <- run_pmslt_lifetable_bau(population, mortality, morbidity, horizon = 1)
calculate_halys(bau)
}
