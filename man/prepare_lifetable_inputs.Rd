% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lifetable-inputs.R
\name{prepare_lifetable_inputs}
\alias{prepare_lifetable_inputs}
\title{Expand age-banded census templates into exact-age lifetable inputs}
\usage{
prepare_lifetable_inputs(
  population = NULL,
  mortality = NULL,
  morbidity = NULL,
  input_dir = NULL
)
}
\arguments{
\item{population, mortality, morbidity}{Data frames or CSV paths in the raw
template format (or already exact-age). Default to the numbered files in
\code{input_dir}. \code{morbidity} is optional.}

\item{input_dir}{Optional directory holding the numbered templates, used to
locate any input not supplied explicitly.}
}
\value{
A list with exact-age \code{population}, \code{mortality}, and \code{morbidity}
(the last \code{NULL} when no morbidity input is available), ready to pass to the
main lifetable functions or \code{\link[=run_pmslt]{run_pmslt()}}.
}
\description{
The all-cause main lifetable engine (\code{\link[=run_pmslt_lifetable_bau]{run_pmslt_lifetable_bau()}} and
\code{\link[=run_pmslt_lifetable_interventions]{run_pmslt_lifetable_interventions()}}) works on exact single-year ages, but
the raw \verb{01_population.csv}, \verb{02_all_cause_mortality.csv}, and
\verb{03_all_cause_morbidity.csv} templates are written in age bands. This helper
bridges the two by expanding each closed age band to single years using an
explicit, documented rule:
}
\details{
\itemize{
\item Population counts (\code{initial_population}) are split \strong{uniformly} across the
single years in the band, so the band total is preserved.
\item Rates (\code{acmr_BAU} mortality, \code{pYLD_BAU} morbidity) are held \strong{constant}
within the band.
}

Tables that already use an exact single-year \code{age} column are returned
unchanged, so the helper is safe to apply to either format.
}
\examples{
\dontrun{
inputs <- prepare_lifetable_inputs(input_dir = "inputs_raw")
run_pmslt_lifetable_bau(inputs$population, inputs$mortality, inputs$morbidity)
}
}
