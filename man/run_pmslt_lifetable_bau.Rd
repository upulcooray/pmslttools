% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main-lifetable.R
\name{run_pmslt_lifetable_bau}
\alias{run_pmslt_lifetable_bau}
\title{Run a deterministic BAU PMSLT all-cause lifetable}
\usage{
run_pmslt_lifetable_bau(
  population,
  mortality,
  morbidity = NULL,
  horizon = NULL,
  spec = NULL
)
}
\arguments{
\item{population}{Data frame or CSV path. Required columns are \code{age}, \code{sex},
\code{stratum}, and \code{population}. The template-style column
\code{initial_population} is also accepted as \code{population}.}

\item{mortality}{Data frame or CSV path. Required columns are \code{age}, \code{sex},
\code{stratum}, and \code{mortality_rate}. The template-style column \code{acmr_BAU} is
also accepted as \code{mortality_rate}. If a \code{time_step} column is present,
mortality rates are matched by \code{time_step}; otherwise baseline rates are
reused every cycle.}

\item{morbidity}{Optional data frame or CSV path. When supplied, required
columns are \code{age}, \code{sex}, \code{stratum}, and \code{morbidity_rate}. The
template-style column \code{pYLD_BAU} is also accepted as \code{morbidity_rate}. If a
\code{time_step} column is present, morbidity rates are matched by \code{time_step};
otherwise baseline rates are reused every cycle.}

\item{horizon}{Number of yearly cycles to run. If omitted, uses
\code{spec$horizon} when \code{spec} is supplied, otherwise defaults to 1.}

\item{spec}{Optional \code{pmslt_spec} object.}
}
\value{
A data frame with class \code{pmslt_lifetable}.
}
\description{
Runs the business-as-usual all-cause lifetable for multiple yearly cycles
using exact single-year integer ages. This function only ages surviving
population forward. It does not add births, migration, entrants, disease
deltas, intervention effects, costs, equity disaggregation, or PSA.
}
\details{
Population ageing is deterministic and transparent. At the next cycle,
population at age \code{a} equals survivors from age \code{a - 1} in the previous
cycle. The minimum starting age receives no new entrants. The maximum age is
currently treated as open-ended: survivors already at the maximum age remain
there and survivors from the previous age also age into the maximum age.
}
\examples{
population <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  population = c(1000, 900)
)
mortality <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  mortality_rate = c(0.01, 0.02)
)
run_pmslt_lifetable_bau(population, mortality, horizon = 2)
}
