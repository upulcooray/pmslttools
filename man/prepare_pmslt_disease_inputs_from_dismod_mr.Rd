% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dismod-mr-to-pmslt.R
\name{prepare_pmslt_disease_inputs_from_dismod_mr}
\alias{prepare_pmslt_disease_inputs_from_dismod_mr}
\title{Convert DisMod-MR outputs to PMSLT disease inputs}
\usage{
prepare_pmslt_disease_inputs_from_dismod_mr(
  dismod_outputs,
  raw_disease_inputs,
  output_path = NULL,
  validate = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{dismod_outputs}{DisMod-MR outputs as a data frame, a CSV path, or a
\code{dismod_mr_outputs} object returned by \code{\link[=read_dismod_mr_outputs]{read_dismod_mr_outputs()}}.}

\item{raw_disease_inputs}{Raw disease epidemiology evidence as a data frame
or a path to \verb{05_disease_epidemiology_raw.csv}. It must contain
\code{disease}, \code{age_start}, \code{age_end}, \code{sex}, \code{stratum}, and
\code{disability_weight}.}

\item{output_path}{Optional CSV path to write the PMSLT-ready disease inputs.}

\item{validate}{Logical. Should the converted table be checked with
\code{\link[=validate_pmslt_disease_inputs]{validate_pmslt_disease_inputs()}}? DisMod-MR output validation is always
performed before conversion.}

\item{overwrite}{Logical. Should \code{output_path} be replaced if it already
exists?}
}
\value{
A data frame with class \code{pmslt_disease_inputs_from_dismod_mr}.
Attributes store the DisMod-MR validation object, a source summary, and the
output path when written.
}
\description{
Bridges validated external DisMod-MR long-format model outputs into the
canonical \code{pmslt_disease_epi.csv} structure used by downstream PMSLT disease
modules. DisMod-MR supplies modelled disease rates; \code{disability_weight} is
joined from raw disease epidemiology evidence.
}
\examples{
\dontrun{
modelled <- read_dismod_mr_outputs(
  "path/to/dismod_mr_results.csv",
  target_grid = "path/to/dismod_mr_inputs/dismod_mr_target_grid.csv"
)
disease_inputs <- prepare_pmslt_disease_inputs_from_dismod_mr(
  modelled,
  raw_disease_inputs = "path/to/raw_inputs/05_disease_epidemiology_raw.csv",
  output_path = "path/to/raw_inputs/pmslt_disease_epi.csv"
)
}
}
