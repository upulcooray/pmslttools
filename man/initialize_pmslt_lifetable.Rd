% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main-lifetable.R
\name{initialize_pmslt_lifetable}
\alias{initialize_pmslt_lifetable}
\title{Initialize a one-step PMSLT all-cause lifetable}
\usage{
initialize_pmslt_lifetable(
  population,
  mortality,
  morbidity = NULL,
  spec = NULL,
  stratum_rate_ratios = NULL
)
}
\arguments{
\item{population}{Data frame or CSV path. Required columns are \code{age}, \code{sex},
\code{stratum}, and \code{population}. The template-style column
\code{initial_population} is also accepted as \code{population}.}

\item{mortality}{Data frame or CSV path. Required columns are \code{age}, \code{sex},
\code{stratum}, and \code{mortality_rate}. The template-style column \code{acmr_BAU} is
also accepted as \code{mortality_rate}.}

\item{morbidity}{Optional data frame or CSV path. When supplied, required
columns are \code{age}, \code{sex}, \code{stratum}, and \code{morbidity_rate}. The
template-style column \code{pYLD_BAU} is also accepted as \code{morbidity_rate}.
When omitted, \code{morbidity_rate} is set to zero.}

\item{spec}{Optional \code{pmslt_spec} object. It is validated when supplied and
stored as an attribute on the result.}

\item{stratum_rate_ratios}{Optional data frame or CSV path from
\verb{11_stratum_rate_ratios.csv}. When supplied, aggregate mortality and
morbidity rates are disaggregated to the population strata before the
one-step lifetable is calculated.}
}
\value{
A data frame with class \code{pmslt_lifetable}.
}
\description{
Creates the business-as-usual starting state for the main all-cause PMSLT
lifetable. This first slice only runs one deterministic time step. It does
not age the population forward, apply disease deltas, model interventions,
add costs, or run PSA. If \code{stratum_rate_ratios} is supplied, aggregate
mortality and morbidity rates are first disaggregated to model strata.
}
\examples{
population <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  population = c(1000, 900)
)
mortality <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  mortality_rate = c(0.01, 0.02)
)
initialize_pmslt_lifetable(population, mortality)
}
