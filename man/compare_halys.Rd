% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main-lifetable.R
\name{compare_halys}
\alias{compare_halys}
\title{Compare HALY-style health outcome summaries}
\usage{
compare_halys(
  bau_results,
  intervention_results,
  by = c("overall", "time_step", "sex", "stratum", "age", "age_band")
)
}
\arguments{
\item{bau_results}{BAU output from \code{\link[=run_pmslt_lifetable_bau]{run_pmslt_lifetable_bau()}} or
\code{\link[=integrate_disease_deltas]{integrate_disease_deltas()}} with \code{person_years} and \code{yld} columns.}

\item{intervention_results}{Intervention output with the same \code{time_step},
\code{age}, \code{sex}, and \code{stratum} structure as \code{bau_results}.}

\item{by}{Character vector of grouping variables. Use \code{"overall"} for one
ungrouped comparison row, or any combination of \code{"time_step"}, \code{"sex"},
\code{"stratum"}, \code{"age"}, and \code{"age_band"}.}
}
\value{
A plain data frame with grouping columns followed by
\code{haly_difference}, \code{person_years_difference}, and \code{yld_difference}.
Integrated disease-total difference columns are included when both inputs
include disease totals.
}
\description{
Compares lightweight HALY summaries from compatible BAU and intervention
PMSLT outputs. Differences are calculated as \code{intervention - BAU}. This is a
reporting helper only and does not add discounting, age weighting, costs,
uncertainty intervals, DALYs, or change the lifetable engine.
}
\examples{
population <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  population = c(1000, 900)
)
mortality <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  mortality_rate = c(0.01, 0.02)
)
morbidity <- data.frame(
  age = c(40L, 41L),
  sex = "female",
  stratum = "total",
  morbidity_rate = c(0.12, 0.15)
)
bau <- run_pmslt_lifetable_bau(population, mortality, morbidity, horizon = 1)
compare_halys(bau, bau)
}
