% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dismod-mr-adapter.R
\name{prepare_dismod_mr_inputs}
\alias{prepare_dismod_mr_inputs}
\title{Prepare external DisMod-MR input files}
\usage{
prepare_dismod_mr_inputs(
  input_dir,
  output_dir = NULL,
  spec = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{input_dir}{Directory containing \verb{05_disease_epidemiology_raw.csv} and
optionally \verb{06_dismod_input_skeleton.csv}.}

\item{output_dir}{Directory where DisMod-MR preparation files should be
written. If \code{NULL}, files are written to \code{dismod_mr_inputs} under
\code{input_dir}.}

\item{spec}{Optional \code{pmslt_spec} object. It is validated when supplied, but
this adapter keeps the target grid anchored to the source CSV rows.}

\item{overwrite}{Logical. Should existing output files be overwritten?}
}
\value{
An object of class \code{dismod_mr_input_preparation} containing the
prepared input data, target grid, omissions audit, summary, file paths, and
output directory.
}
\description{
Converts package raw disease evidence into clean CSV files for an external
DisMod-MR workflow. This function does not run DisMod-MR and does not create
\code{pmslt_disease_epi.csv}; it only prepares package-side input and audit files.
}
\examples{
\dontrun{
prep <- prepare_dismod_mr_inputs("path/to/raw_inputs")
prep
}
}
