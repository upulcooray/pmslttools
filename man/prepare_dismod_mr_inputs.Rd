% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dismod-mr-inputs.R
\name{prepare_dismod_mr_inputs}
\alias{prepare_dismod_mr_inputs}
\title{Prepare external DisMod-MR input files}
\usage{
prepare_dismod_mr_inputs(
  input_dir = NULL,
  output_dir,
  spec = NULL,
  disease_raw = NULL,
  dismod_skeleton = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{input_dir}{Directory containing raw PMSLT disease CSV files. Required
unless direct overrides are supplied for \code{disease_raw} or
\code{dismod_skeleton}.}

\item{output_dir}{Directory where DisMod-MR preparation files should be
written.}

\item{spec}{Optional \code{pmslt_spec} object. When supplied, exact target ages
are derived from \code{spec$ages}.}

\item{disease_raw}{Optional raw disease evidence as a data frame or a path to
\verb{05_disease_epidemiology_raw.csv}.}

\item{dismod_skeleton}{Optional long-format DisMod skeleton evidence as a
data frame or a path to \verb{06_dismod_input_skeleton.csv}.}

\item{overwrite}{Logical. Should existing output files be overwritten?}
}
\value{
A list with prepared \code{evidence}, \code{target_grid}, \code{omissions},
\code{summary}, and \code{files}. The object has class
\code{dismod_mr_input_preparation}.
}
\description{
Converts raw PMSLT disease evidence into explicit CSV files for an external
DisMod-MR workflow. This function prepares file contracts only: it does not
run DisMod-MR and it does not create the canonical PMSLT-ready
\code{pmslt_disease_epi.csv}.
}
\details{
The exporter reads \verb{05_disease_epidemiology_raw.csv} and, when present,
\verb{06_dismod_input_skeleton.csv}. Direct data-frame or CSV-path overrides can
be supplied with \code{disease_raw} and \code{dismod_skeleton}. When both sources
contain the same disease, sex, stratum, age group, and parameter, the
skeleton row is kept and the raw row is recorded in the omissions audit as
superseded.

The function writes four files:
\code{dismod_mr_input_long.csv}, \code{dismod_mr_target_grid.csv},
\code{dismod_mr_input_omissions.csv}, and \code{dismod_mr_input_summary.csv}.
}
\examples{
\donttest{
input_dir <- file.path(tempdir(), "pmslt_raw_inputs")
output_dir <- file.path(tempdir(), "dismod_mr_inputs")
dir.create(input_dir, showWarnings = FALSE)
disease_raw <- data.frame(
  disease = "ihd",
  sex = "female",
  stratum = "total",
  age_start = 45,
  age_end = 49,
  age_label = "45-49",
  incidence_rate = 0.01,
  prevalence = 0.05,
  remission_rate = 0.02,
  excess_mortality_rate = 0.10,
  case_fatality_rate = 0.03
)
utils::write.csv(
  disease_raw,
  file.path(input_dir, "05_disease_epidemiology_raw.csv"),
  row.names = FALSE
)
prep <- prepare_dismod_mr_inputs(input_dir, output_dir, overwrite = TRUE)
prep$files
}
}
