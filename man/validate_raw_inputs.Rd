% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw-validation.R
\name{validate_raw_inputs}
\alias{validate_raw_inputs}
\title{Validate raw PMSLT input templates}
\usage{
validate_raw_inputs(input_dir, spec = NULL)
}
\arguments{
\item{input_dir}{Directory containing raw input CSV files.}

\item{spec}{Optional \code{pmslt_spec} object used to determine which optional
template files and generated values are expected. If omitted, the function
uses \verb{00_model_specification.csv} when possible.}
}
\value{
A data frame with class \code{pmslt_validation_issues} and columns
\code{file}, \code{row}, \code{column}, \code{severity}, \code{message}, and \code{suggested_fix}.
}
\description{
Checks raw CSV files generated by \code{\link[=draft_input_templates]{draft_input_templates()}} before DisMod
processing. The validation is schema-driven and returns all issues it can
find instead of stopping at the first problem.
}
\examples{
\dontrun{
spec <- pmslt_spec(
  intervention = "Tobacco tax",
  mechanism = "direct",
  diseases = "CHD"
)
draft_input_templates(spec, output_dir = tempdir(), overwrite = TRUE)
issues <- validate_raw_inputs(tempdir(), spec)
issues <- validate_raw_inputs(tempdir())
}
}
